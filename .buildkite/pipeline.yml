# This pipeline will trigger another pipeline on pi-gen repo
- trigger: "build-susibian"
  label: ":cherries: Build SUSIbian"
  build:
    branch: "susi-buildkite"
    message: Continue from "${BUILDKITE_MESSAGE}"
